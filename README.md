# OpenWeather CLI

Скрипт получает текущую погоду по названию города через OpenWeatherMap API и выводит в консоль температуру в °C и описание погоды.

## Установка

1. Убедитесь, что установлен Python 3.9+.
2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` в корне проекта на основе `.env.example` и укажите ваш API-ключ:

```env
OPENWEATHER_API_KEY=ваш_ключ
```

## Запуск

```bash
python weather.py Moscow
```

Можно передавать города из нескольких слов:

```bash
python weather.py "New York"
```

## Обработка ошибок

- Если город не найден, выводится понятное сообщение об ошибке.
- Если API вернул ошибку (например, невалидный ключ), выводится код и текст ошибки.
- Если ключ отсутствует в `.env`, скрипт сообщает об этом и завершает работу без падения.
